import React, {FC} from 'react';

interface ErrorPageProps {
}

export const ErrorPage: FC<ErrorPageProps> = ({}) => {
    return (
        <div>
            To by bylo na tyle
        </div>
    );
};